<div class="row">
  <div class="col-md-6 col-md-offset-3">

  	<div class="profile">

<<<<<<< HEAD
  		<div class="profile-image">
  		</div>
  		
  		<div class="profile-desc">

			<h2>Username</h2>
			<%= @user.name %>
			<h2>Email</h2>
			<%= @user.email %>  
			<h2>Information</h2>
			<%= @user.info %>
			<h2>Rating</h2>
			★ ★ ★ ★ ☆

  		</div>
	
  	</div>

  	<!-- if this is a guide, display comments -->
  	<% if ( @user.property == 1 ) %>
	<h2>Comments</h2>
	<div class="comment-container">
		<h3>John Smith</h3>
		<div class="comment">★ ★ ★ ★ ☆ Thanks for a good time!</div>
		<h3>Jane Sans</h3>
		<div class="comment">★ ★ ★ ★ ☆ Thanks for a good time!</div>
		<h3>Jerry Serif</h3>
		<div class="comment">★ ★ ★ ★ ☆ Thanks for a good time!</div>
	</div>
	<% end %>

	</div>
</div>
=======
  <br>
<% @user.packages.each do |package| %>
<div class = "col-md-12">
 <div class = "col-md-4">
 <p>package name:</p>
  <%= link_to package.name, package %>
  </div>
   <div class = "col-md-4">
   <p>package price:</p>
   <%= package.price %>
     </div>
   <div class = "col-md-4">

    <p>package time:</p>
   <%= package.time %>
        </div>

    <% package.items.each do |item| %>
    
      <div class = "col-md-3">
       <p>item name:</p>
       <%= item.name %> 
      </div>

      <div class = "col-md-3">
       <p>item price:</p>
       <%= item.price %> 
      </div>

      <div class = "col-md-3">
       <p>item time:</p>
       <%= item.time %>
      </div>
      
      <div class = "col-md-3">
       <p>item info:</p>
       <%= simple_format(item.info)%>
      </div>

   <% end %>
</div>   
     <br>
     <br>

<h1><strong>create item for this package</strong></h1>
    <%= form_for(package.items.build) do |f| %>

    <%= f.label :itemsName %>
    <%= f.text_field :name, class: 'form-control' %>

    <%= f.label :itemsPrice %>
    <%= f.number_field :price, class: 'form-control' %>
    
    <%= f.label :itemsTime %>
    <%= f.number_field :time, class: 'form-control' %>

    <%= f.label :itemsInfo %>
    <%= f.text_area :info, class: 'form-control' %>

    <%= hidden_field_tag :package_id, package.id %>
    
    <%= f.submit "Add to author", class: "btn btn-primary" %>
  <% end %>
<% end %>
  <br>

<ul>
 <h1><strong>create package</strong></h1>

 <%= form_for(@user.packages.build) do |f| %>

    <%= f.label :Name %>
    <%= f.text_field :name, class: 'form-control' %>

    <%= f.label :Price %>
    <%= f.number_field :price, class: 'form-control' %>
    
    <%= f.label :Time %>
    <%= f.number_field :time, class: 'form-control' %>

    <%= hidden_field_tag :user_id, @user.id %>
    
    <%= f.submit "Add to author", class: "btn btn-primary" %>
  <% end %>
</ul>
>>>>>>> package
